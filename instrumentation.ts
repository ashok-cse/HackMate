export async function register() {
  console.error(
    "[hackmate:boot] Server instrumentation loaded — Easypanel Logs tab streams this process; Terminal is a separate shell and does not show node server.js output.",
  );
}
